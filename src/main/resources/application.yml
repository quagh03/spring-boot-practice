server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_practice_ecommerce
    username: root
    password: lequanghuy2212
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
open:
  api:
    serverUrl: "http://localhost:8080/"
    serverName: "Development Server"
    title: "Ecommerce API"
    version: "1.0"
    description: "A simple CRUD API for Ecommerce"
api:
  prefix: api/v1